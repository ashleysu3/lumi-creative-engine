{
  "name": "@adsbylumi/creative-engine",
  "version": "0.7.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^4.0.0",
    "wrangler": "^4.77.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
